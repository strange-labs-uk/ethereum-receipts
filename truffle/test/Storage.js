var Storage = artifacts.require("Storage");

contract('Storage', function(accounts) {

    it("should write value if key does not exist", (done) => {
    }); 

    it("should overwrite value if key does exist", (done) => {
    }); 

    it("should return the value if key does exist", (done) => {
    }); 

    it("should return an error if key does not exist", (done) => {
    }); 

    it("should update the controller address if owned", (done) => {
    });

    it("should not update the controller address if not owned", (done) => {
    });

});